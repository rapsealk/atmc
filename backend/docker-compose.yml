version: "3"

services:
  django:
    image: atmc/backend:0.1.0-dev
    container_name: atmc-backend-dev
    build:
      context: .
      dockerfile: ./Dockerfile
    restart: unless-stopped
    volumes:
      - .:/app:z
    ports:
      - 8000:8000
    command: /usr/bin/docker-start.sh
